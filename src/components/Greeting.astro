<section class="h-screen flex flex-col justify-center items-center">
	<div class="relative h-[60vh] w-fit rounded-b-3xl overflow-hidden">
		<span
			class="w-full h-[85%] absolute bottom-0 bg-slate-600 bg-opacity-50 backdrop-blur-md rounded-t-full"
		></span>
		<img
			class="relative"
			src="./agt_1.png"
			alt="Joven con bucket hat beige sonriendo de perfil"
		/>
	</div>

	<h1 class="text-9xl font-extrabold text-center text-white opacity-50">Adán González</h1>
	<h2 class="text-6xl font-extrabold text-center">Fullstack👨🏽‍💻Developer</h2>
</section>

<style>

	@supports (-webkit-text-stroke: 3px rgb(var(--foreground-rgb))) {
		h2 {
			-webkit-text-fill-color: transparent;
			-webkit-text-stroke: 1.5px rgb(var(--foreground-rgb));
		}
	}

	@supports not (-webkit-text-stroke: 3px rgb(var(--foreground-rgb))) {
		h2 {
			color: rgba(var(--foreground-rgb), 0.5);
			text-shadow:
				1px 1px 0 rgb(var(--foreground-rgb)),
				-1px 1px 0 rgb(var(--foreground-rgb)),
				-1px -1px 0 rgb(var(--foreground-rgb)),
				1px -1px 0 rgb(var(--foreground-rgb));
		}
	}
</style>
